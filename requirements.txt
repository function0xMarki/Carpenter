# Carpenter - Python Dependencies
# ================================
# Required for password-protected ZIP split/join (AES-256 compatible with 7-Zip)
pyzipper>=0.3.6

# System dependency (optional, only for interoperability checks/manual verification)
# macOS:   brew install p7zip
# Ubuntu:  sudo apt install p7zip-full
# Fedora:  sudo dnf install p7zip p7zip-plugins
# Arch:    sudo pacman -S p7zip
