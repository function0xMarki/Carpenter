# Carpenter - Python Dependencies
# ================================
# Core functionality uses only the Python standard library.

# Optional, required only for password protection (AES-256 ZIP).
# Offered for automatic installation on first run if missing.
pyzipper
