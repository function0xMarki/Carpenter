# Carpenter - Dependencias de Python
# ===================================
# Requerido para dividir/unir ZIP protegidos con contraseña
# (AES-256 compatible con 7-Zip)
pyzipper>=0.3.6

# Dependencia del sistema (opcional, solo para verificar interoperabilidad)
# macOS:   brew install p7zip
# Ubuntu:  sudo apt install p7zip-full
# Fedora:  sudo dnf install p7zip p7zip-plugins
# Arch:    sudo pacman -S p7zip
